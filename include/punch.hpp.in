#pragma once

#include <filesystem>
namespace fs = std::filesystem;

namespace paths {
    inline fs::path const project_dir{"@knockout_SOURCE_DIR@"};
    inline fs::path const punchcard_dir = project_dir/"punch-cards";
}
